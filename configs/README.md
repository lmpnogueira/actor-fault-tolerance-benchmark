# Configuration Example

This directory contains an example benchmark configuration (`config.yml`).

The file illustrates the YAML structure expected by the benchmark implementations and can be used as a starting point for creating custom benchmark scenarios.

The complete set of configurations used in the experimental campaign is generated automatically by the scripts in the `stats/` directory and stored in `stats/generated_configs/`.